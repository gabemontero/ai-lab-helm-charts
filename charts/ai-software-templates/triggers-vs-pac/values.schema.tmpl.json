{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "tekton": {
      "type": "object",
      "properties": {
        "gitCredentials": {
          "description": "[REQUIRED] The value of the '.git-credentials' key of the Secret used by the git-clone task.",
          "type": "string"
        },
        "gitConfig": {
          "description": "[REQUIRED] The value of the '.gitconfig' key of the Secret used by the git-clone task.",
          "type": "string"
        },
        "gitProviderToken": {
          "description": "[REQUIRED] The value of the 'git-provider-token' key of the Secret used by the git-clone task.",
          "type": "string"
        },
        "quayAccountName": {
          "description": "[REQUIRED] The quay.io account that the application image will be pushed.",
          "title": "Quay Account Name",
          "type": "string"
        },
        "applicationName": {
          "description": "[REQUIRED] The name of the application that will leverage Tekton Triggers to handle GitHub push events.",
          "type": "string"
        }
      },
      "required": ["applicationName", "quayAccountName", "gitCredentials", "gitConfig", "gitProviderToken"]
    }
  }
}
